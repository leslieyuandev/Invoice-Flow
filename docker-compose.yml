services:
  # ── PostgreSQL (local dev database) ─────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-invoice_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-invoice_pass}
      POSTGRES_DB: ${POSTGRES_DB:-invoice_app}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-invoice_user} -d ${POSTGRES_DB:-invoice_app}"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ── Next.js application ──────────────────────────────────────────────────────
  app:
    image: ghcr.io/leslieyuandev/invoice-flow:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    restart: unless-stopped
    # This instance shares the main app's database (Vercel owns migrations), so skip
    # `prisma migrate deploy` on startup and just serve.
    command: ["sh", "-c", "npm run start"]
    # Headless Chromium needs more shared memory than Docker's 64MB default.
    shm_size: "1gb"
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://invoice_user:invoice_pass@db:5432/invoice_app}
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:3000}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
      RESEND_API_KEY: ${RESEND_API_KEY}
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      STORAGE_BUCKET: ${STORAGE_BUCKET:-}
      STORAGE_ENDPOINT: ${STORAGE_ENDPOINT:-}
      STORAGE_ACCESS_KEY: ${STORAGE_ACCESS_KEY:-}
      STORAGE_SECRET_KEY: ${STORAGE_SECRET_KEY:-}
      STORAGE_PUBLIC_URL: ${STORAGE_PUBLIC_URL:-}
      # This instance only exposes the self-hosted scraper tools — Maps Extractor +
      # Instagram Hashtag Scraper (full app lives on the main deployment).
      MAPS_ONLY: ${MAPS_ONLY:-true}
      # Maps Extractor multi-user knobs (see DEPLOY.md)
      MAPS_MAX_CONCURRENCY: ${MAPS_MAX_CONCURRENCY:-2}
      MAPS_MAX_PER_USER: ${MAPS_MAX_PER_USER:-1}
      MAPS_PROXY_SERVER: ${MAPS_PROXY_SERVER:-}
      MAPS_PROXY_USERNAME: ${MAPS_PROXY_USERNAME:-}
      MAPS_PROXY_PASSWORD: ${MAPS_PROXY_PASSWORD:-}
      # Instagram Hashtag Scraper — provider tokens (free-credits only; see .env.example)
      APIFY_TOKEN: ${APIFY_TOKEN:-}
      APIFY_FREE_CREDITS: ${APIFY_FREE_CREDITS:-1000}
      BRIGHTDATA_TOKEN: ${BRIGHTDATA_TOKEN:-}
      BRIGHTDATA_IG_HASHTAG_DATASET_ID: ${BRIGHTDATA_IG_HASHTAG_DATASET_ID:-}
      BRIGHTDATA_FREE_CREDITS: ${BRIGHTDATA_FREE_CREDITS:-500}
      SCRAPINGBEE_API_KEY: ${SCRAPINGBEE_API_KEY:-}
      SCRAPINGBEE_FREE_CREDITS: ${SCRAPINGBEE_FREE_CREDITS:-1000}
      CRAWLBASE_TOKEN: ${CRAWLBASE_TOKEN:-}
      CRAWLBASE_FREE_CREDITS: ${CRAWLBASE_FREE_CREDITS:-1000}
      INSTAGRAM_MAX_CONCURRENCY: ${INSTAGRAM_MAX_CONCURRENCY:-3}
      INSTAGRAM_MAX_PER_USER: ${INSTAGRAM_MAX_PER_USER:-1}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./public:/app/public

  # ── Caddy reverse proxy: automatic HTTPS for your domain ─────────────────────
  # Opt-in: only starts with `--profile proxy`. Requires DOMAIN to be set and the
  # domain's DNS A record pointed at this server's public IP (ports 80 + 443 open).
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    profiles: ["proxy"]
    ports:
      - "80:80"
      - "443:443"
    environment:
      # Required when using `--profile proxy`; set DOMAIN=app.example.com (see DEPLOY.md).
      DOMAIN: ${DOMAIN:-}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - app

  # ── Cloudflare Tunnel: serve a domain from this machine, no VPS / open ports ──
  # Opt-in: only starts with `--profile tunnel`. Create a named tunnel in the
  # Cloudflare Zero Trust dashboard, route your hostname to service http://app:3000,
  # copy the token, set CLOUDFLARE_TUNNEL_TOKEN, then `docker compose --profile tunnel up -d`.
  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    profiles: ["tunnel"]
    # Required when using `--profile tunnel`; set CLOUDFLARE_TUNNEL_TOKEN (see DEPLOY.md).
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN:-}
    depends_on:
      - app

volumes:
  postgres_data:
  caddy_data:
  caddy_config:
